syntax = "proto3";

package message.control;

import "google/protobuf/timestamp.proto";
import "Vector.proto";

message TauOveride
{
    google.protobuf.Timestamp timestamp = 1;
    vec3 value = 2; // Surge Sway Yaw
    bool overide_surge = 3;
    bool overide_sway = 4;
    bool overide_yaw = 5;
}
